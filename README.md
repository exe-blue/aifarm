# DoAi.Me - AI Farm Orchestration System

> 300대 규모의 안드로이드 디바이스 팜 자동화 및 YouTube 시청 워크로드 관리 시스템

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![Node.js](https://img.shields.io/badge/Node.js-20+-green.svg)](https://nodejs.org/)
[![Next.js](https://img.shields.io/badge/Next.js-15-black.svg)](https://nextjs.org/)
[![Supabase](https://img.shields.io/badge/Supabase-PostgreSQL-3ECF8E.svg)](https://supabase.com/)

---

## 📋 목차

1. [시스템 개요](#-시스템-개요)
2. [아키텍처](#-아키텍처)
3. [디바이스 계층 구조](#-디바이스-계층-구조)
4. [핵심 컴포넌트](#-핵심-컴포넌트)
5. [워크로드 시스템](#-워크로드-시스템)
6. [Quick Start](#-quick-start)
7. [프로젝트 구조](#-프로젝트-구조)
8. [문서](#-문서)

---

## 🎯 시스템 개요

DoAi.Me는 **5대의 워크스테이션**에서 **300대의 Galaxy S9 스마트폰**을 관리하여 YouTube 영상 시청을 자동화하는 시스템입니다.

### 주요 기능

| 기능 | 설명 |
|------|------|
| **디바이스 관리** | 폰보드-슬롯 기반 300대 기기 등록/모니터링 |
| **워크로드 실행** | 영상 리스팅 → 50% 배치 실행 → 결과 기록 사이클 |
| **Laixi 연동** | PC 앱을 통한 Android 기기 제어 (터치, 스와이프, ADB) |
| **실시간 모니터링** | WebSocket 기반 상태 추적 및 대시보드 |
| **히스토리 관리** | 명령/시청/워크로드 내역 조회 및 분석 |

### 인프라 스펙

```
워크스테이션: 5대 (WS01 ~ WS05)
├─ VLAN: 6개 (네트워크 분리)
├─ 폰보드/워크스테이션: 3대
└─ 디바이스/폰보드: 20대 (Galaxy S9)

총 디바이스: 5 × 3 × 20 = 300대
```

---

## 🏗️ 아키텍처

### 전체 시스템 흐름

```
                              ┌──────────────────────────────────────────────────────────┐
                              │                      CLOUD                                │
                              │                                                          │
 ┌─────────────┐              │  ┌────────────┐    ┌────────────┐    ┌────────────┐     │
 │   Admin     │──────────────┼─▶│   Vercel   │───▶│   Vultr    │───▶│  Supabase  │     │
 │  (Browser)  │  HTTPS       │  │  (Next.js) │ API│  (FastAPI) │ DB │ (PostgreSQL)│     │
 └─────────────┘              │  └────────────┘    └─────┬──────┘    └────────────┘     │
                              │                          │ WSS                           │
                              │                          │ Tailscale VPN                 │
                              └──────────────────────────┼───────────────────────────────┘
                                                         │
┌────────────────────────────────────────────────────────┼─────────────────────────────────┐
│                              LOCAL (Workstation)       │                                  │
│                                                        │                                  │
│  ┌─────────────────────────────────────────────────────┴─────────────────────────────┐  │
│  │                         Node Runner (Python)                                       │  │
│  │                                                                                    │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │  │
│  │  │ DeviceSync   │  │ WorkloadExec │  │ LaixiClient  │  │ BatchExecutor│          │  │
│  │  │ (Supabase)   │  │   (Engine)   │  │  (WebSocket) │  │  (50% Split) │          │  │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │  │
│  └─────────┼─────────────────┼─────────────────┼─────────────────┼───────────────────┘  │
│            │                 │                 │                 │                      │
│            │                 │                 │                 │                      │
│  ┌─────────┼─────────────────┼─────────────────┼─────────────────┼───────────────────┐  │
│  │         ▼                 │                 ▼                 │                   │  │
│  │  ┌─────────────┐          │         ┌─────────────┐           │                   │  │
│  │  │    ADB      │◀─────────┼─────────│  Laixi.exe  │◀──────────┘                   │  │
│  │  │   Server    │          │         │  (PC App)   │                               │  │
│  │  └──────┬──────┘          │         └──────┬──────┘                               │  │
│  │         │ USB             │                │ USB/WiFi                             │  │
│  │         ▼                 │                ▼                                      │  │
│  │  ┌───────────────────────────────────────────────────────────────────────────┐   │  │
│  │  │                      Phone Boards (3개)                                    │   │  │
│  │  │  ┌─────────────────────────────────────────────────────────────────────┐  │   │  │
│  │  │  │ PB01: [S01][S02][S03][S04][S05]...[S20]  ← 20 Slots (Galaxy S9)     │  │   │  │
│  │  │  └─────────────────────────────────────────────────────────────────────┘  │   │  │
│  │  │  ┌─────────────────────────────────────────────────────────────────────┐  │   │  │
│  │  │  │ PB02: [S01][S02][S03][S04][S05]...[S20]  ← 20 Slots (Galaxy S9)     │  │   │  │
│  │  │  └─────────────────────────────────────────────────────────────────────┘  │   │  │
│  │  │  ┌─────────────────────────────────────────────────────────────────────┐  │   │  │
│  │  │  │ PB03: [S01][S02][S03][S04][S05]...[S20]  ← 20 Slots (Galaxy S9)     │  │   │  │
│  │  │  └─────────────────────────────────────────────────────────────────────┘  │   │  │
│  │  └───────────────────────────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

### 연결 방식

| 계층 | 프로토콜 | 설명 |
|------|----------|------|
| Admin → Cloud | HTTPS | Web Dashboard 접속 |
| Cloud → Local | WSS (Tailscale) | 명령 전달, 상태 동기화 |
| Local → Laixi | WebSocket (22221) | 기기 제어 명령 |
| Local → ADB | USB | 직접 ADB 명령 |
| ADB → Device | USB | 앱 실행, 파일 전송 |

---

## 📱 디바이스 계층 구조

### 명명 규칙

```
{워크스테이션ID}-{폰보드ID}-{슬롯번호}
예: WS01-PB02-S15
    └─WS01: 워크스테이션 1번
       └─PB02: 폰보드 2번
          └─S15: 슬롯 15번
```

### 계층 상세

```
DoAi.Me Device Farm
│
├─ WS01 (워크스테이션 1, VLAN 1)
│  ├─ WS01-PB01 (폰보드 1)
│  │  ├─ WS01-PB01-S01 (슬롯 1, Group A)
│  │  ├─ WS01-PB01-S02 (슬롯 2, Group B)
│  │  ├─ ...
│  │  └─ WS01-PB01-S20 (슬롯 20, Group B)
│  ├─ WS01-PB02 (폰보드 2)
│  └─ WS01-PB03 (폰보드 3)
│
├─ WS02 (워크스테이션 2, VLAN 2)
│  └─ ...
│
├─ WS03, WS04, WS05
│
└─ 총 300대 (5 × 3 × 20)
```

### 그룹 분할 (50% 배치 실행)

디바이스는 슬롯 번호에 따라 **A/B 그룹**으로 자동 분할됩니다:
- **Group A**: 홀수 슬롯 (S01, S03, S05...)
- **Group B**: 짝수 슬롯 (S02, S04, S06...)

워크로드 실행 시 Group A → 대기 → Group B 순으로 실행하여 부하를 분산합니다.

---

## 🔧 핵심 컴포넌트

### 1. Laixi (PC 앱)

> Android 기기 제어를 위한 Windows PC 앱

```
위치: C:\Program Files\Laixi\Laixi.exe
포트: ws://127.0.0.1:22221/
프로토콜: JSON over WebSocket
```

**주요 기능:**
- 다중 기기 제어 (터치, 스와이프)
- 클립보드 조작 (한글 입력 지원)
- ADB 명령 실행
- 스크린샷 캡처

**관련 코드:** [`shared/laixi_client.py`](shared/laixi_client.py)

### 2. DeviceRegistry (Python)

> 폰보드-슬롯 기반 디바이스 관리

```python
from shared.device_registry import get_device_registry

registry = get_device_registry()

# 디바이스 등록
await registry.register_device(
    serial="ABC123",
    workstation="WS01",
    board=2,
    slot=15
)  # → WS01-PB02-S15

# 50% 배치 그룹 조회
group_a, group_b = await registry.get_batch_groups()
```

**관련 코드:** [`shared/device_registry.py`](shared/device_registry.py)

### 3. WorkloadEngine (Python)

> 워크로드 실행 사이클 관리

```
영상 리스팅 → 명령 → 결과 기록 → 대기 → 반복
   LISTING  → EXECUTING → RECORDING → WAITING → ...
```

**관련 코드:** [`shared/workload_engine.py`](shared/workload_engine.py)

### 4. BatchExecutor (Python)

> 50% 배치 실행 로직

```python
from shared.batch_executor import get_batch_executor

executor = get_batch_executor()

# 절반씩 2회 실행
results = await executor.execute_half_batches(context)
# 1차: Group A (150대) → 60초 대기 → 2차: Group B (150대)
```

**관련 코드:** [`shared/batch_executor.py`](shared/batch_executor.py)

### 5. Supabase Client (Python)

> 직접 DB 동기화 (서버 우회)

```python
from shared.supabase_client import DeviceSync, JobSync

# 기기 상태 동기화
sync = DeviceSync(pc_id=1)
await sync.upsert_device("serial123", status="idle", model="Galaxy S9")

# 작업 상태 관리
jobs = JobSync(pc_id=1)
await jobs.complete_job(job_id, watch_time=120)
```

**관련 코드:** [`shared/supabase_client.py`](shared/supabase_client.py)

---

## 🔄 워크로드 시스템

### 워크로드란?

**워크로드(Workload)** = 하나 이상의 YouTube 영상을 300대 디바이스에서 시청하는 작업 단위

### 실행 사이클

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Workload Cycle                                  │
│                                                                      │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐      │
│  │ LISTING  │───▶│EXECUTING │───▶│RECORDING │───▶│ WAITING  │──┐   │
│  │          │    │          │    │          │    │          │  │   │
│  │ 다음영상  │    │ 50%배치  │    │ DB저장   │    │ 대기시간 │  │   │
│  │ 선택     │    │ 실행     │    │ 히스토리  │    │ (5분)    │  │   │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘  │   │
│       ▲                                                         │   │
│       └─────────────────────────────────────────────────────────┘   │
│                        다음 영상으로 반복                            │
│                                                                      │
│  ┌──────────┐                                                       │
│  │COMPLETED │  ← 모든 영상 처리 완료                                │
│  └──────────┘                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

### 50% 배치 실행

```
┌────────────────────────────────────────────────────────────────┐
│                   Batch Execution (50%)                         │
│                                                                 │
│  Round 1: Group A (150대)                                       │
│  ┌─────┬─────┬─────┬─────┬─────┐                              │
│  │S01  │S03  │S05  │...  │S19  │ ← 홀수 슬롯                   │
│  └─────┴─────┴─────┴─────┴─────┘                              │
│           │                                                     │
│           ▼ 60초 대기 (batch_interval)                         │
│           │                                                     │
│  Round 2: Group B (150대)                                       │
│  ┌─────┬─────┬─────┬─────┬─────┐                              │
│  │S02  │S04  │S06  │...  │S20  │ ← 짝수 슬롯                   │
│  └─────┴─────┴─────┴─────┴─────┘                              │
└────────────────────────────────────────────────────────────────┘
```

### 왜 50%씩 실행하는가?

1. **부하 분산**: Laixi/네트워크 동시 부하 감소
2. **오류 격리**: 한 그룹 실패 시 다른 그룹 보존
3. **자연스러운 트래픽**: 점진적 시청 패턴 생성

---

## 🚀 Quick Start

### 필수 요구사항

- Python 3.11+
- Node.js 20+
- Docker & Docker Compose
- ADB (Android Debug Bridge)
- Laixi.exe (Windows)

### 1. 환경 설정

```bash
# 저장소 클론
git clone https://github.com/exe-blue/doai-me.git
cd doai-me

# 환경 변수 설정
cp env.example .env
# .env 파일 수정 (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY 등)
```

### 2. 로컬 개발 실행

```bash
# Docker로 전체 서비스 실행
docker-compose up -d

# 또는 개별 실행

# Backend API
cd services/api
pip install -r requirements.txt
uvicorn main:app --reload --port 8001

# Frontend (Next.js)
cd apps/web
npm install
npm run dev

# NodeRunner (워크스테이션에서)
cd local/node-runner
pip install -r requirements.txt
python noderunner.py
```

### 3. 기기 연결

```bash
# 1. Laixi.exe 실행 (Windows)
# C:\Program Files\Laixi\Laixi.exe

# 2. ADB로 기기 연결 확인
adb devices

# 3. 기기 등록 (Python)
python -c "
import asyncio
from shared.device_registry import get_device_registry

async def register():
    registry = get_device_registry()
    await registry.register_device(
        serial='YOUR_SERIAL',
        workstation='WS01',
        board=1,
        slot=1
    )
    print('등록 완료!')

asyncio.run(register())
"
```

---

## 📁 프로젝트 구조

```
aifarm/
├── 📱 apps/                     # 프론트엔드 애플리케이션
│   └── web/                     # Next.js 메인 웹앱 (Market, Admin)
│
├── ☁️ services/                 # 백엔드 서비스
│   └── api/                     # FastAPI 백엔드
│
├── 🔧 shared/                   # 공유 Python 코드
│   ├── laixi_client.py         # Laixi WebSocket 클라이언트
│   ├── supabase_client.py      # Supabase 직접 클라이언트
│   ├── device_registry.py      # 디바이스 레지스트리
│   ├── workload_engine.py      # 워크로드 엔진
│   ├── batch_executor.py       # 배치 실행기
│   ├── schemas/                 # Pydantic 스키마
│   └── database/                # SQL 마이그레이션
│
├── 📦 sdk/                      # 외부 SDK (Laixi API 예제)
│
├── 📚 docs/                     # 문서
│   ├── guides/                  # 설정 가이드
│   ├── runbooks/                # 운영 런북
│   └── specs/                   # 기술 스펙
│
├── 🧪 tests/                    # 테스트
├── 📜 scripts/                  # 유틸리티 스크립트
│
├── docker-compose.yml           # Docker 설정
├── pyproject.toml               # Python 프로젝트 설정
└── README.md                    # 이 파일
```

---

## 📖 문서

### 설정 가이드

| 문서 | 설명 |
|------|------|
| [QUICKSTART.md](docs/guides/QUICKSTART.md) | 최초 사용자 빠른 시작 |
| [DEVICE_HIERARCHY.md](docs/guides/DEVICE_HIERARCHY.md) | 디바이스 계층 구조 상세 |
| [WORKLOAD_SYSTEM.md](docs/guides/WORKLOAD_SYSTEM.md) | 워크로드 시스템 상세 |
| [Laixi Integration](docs/LAIXI_INTEGRATION.md) | Laixi 연동 가이드 |
| [Admin Setup](docs/guides/ADMIN_SETUP.md) | 관리자 대시보드 설정 |

### 운영 런북

| 상황 | 런북 |
|------|------|
| 서버 복구 | [recover.md](docs/runbooks/recover.md) |
| ADB 문제 | [adb.md](docs/runbooks/adb.md) |
| Caddy 설정 | [caddy.md](docs/runbooks/caddy.md) |
| Tailscale | [tailscale.md](docs/runbooks/tailscale.md) |

### API 문서

| 문서 | 설명 |
|------|------|
| [API Spec](docs/api.md) | REST/WebSocket API 명세 |
| [Gateway Protocol](docs/GATEWAY_PROTOCOL_V2.md) | Gateway 통신 프로토콜 |

---

## 🔐 보안

- **Tailscale VPN**: 노드-서버 간 암호화 통신
- **Service Role Key**: Supabase 백엔드 전용 키
- **환경 변수**: 민감 정보는 `.env`에만 저장

자세한 내용: [Security Guide](docs/security.md)

---

## 🤝 기여 가이드

1. `feature/*` 브랜치에서 작업
2. PR 템플릿 작성
3. 테스트 통과 확인
4. 리뷰 후 main에 머지

**⚠️ main 직접 푸시 금지!**

---

## 📝 라이선스

Private - All Rights Reserved

---

*Built with ❤️ by DoAi.Me Team*
