# KPI 및 대시보드 요구사항

**생성일**: 2025-12-28
**용도**: 성과 측정 지표 및 웹 대시보드 설계

---

## 핵심 KPI

### 일일 지표

| 지표 | 목표 | 최소 | 우수 | 측정 방법 |
|------|-----|-----|-----|---------|
| 총 활동 수 | 10,000건 | 8,000 | 12,000+ | SUM(all_activities) |
| 발견물 수 | 500건 | 400 | 600+ | COUNT(discoveries WHERE score >= 6.0) |
| 댓글 수 | 2,500건 | 2,000 | 3,000+ | COUNT(comments_posted) |
| 디바이스 가동률 | 95% | 90% | 98%+ | active_devices / 600 * 100 |
| 평균 발견물 점수 | 7.5 | 7.0 | 8.0+ | AVG(discovery_score) |
| 에러율 | < 5% | < 8% | < 3% | errors / total_tasks * 100 |

**SQL 쿼리 예시**:
```sql
-- 일일 통계
SELECT
    DATE(discovered_at) as date,
    COUNT(*) as total_discoveries,
    AVG(score) as avg_score,
    COUNT(CASE WHEN discovery_type = 'viral_short' THEN 1 END) as viral_shorts,
    COUNT(CASE WHEN discovery_type = 'trending_video' THEN 1 END) as trending_videos
FROM discoveries
WHERE discovered_at >= CURRENT_DATE
GROUP BY DATE(discovered_at);
```

---

### 주간 지표

| 지표 | 목표 | 측정 방법 |
|------|-----|---------|
| 트렌드 예측 정확도 | > 60% | validated_trends / total_predictions * 100 |
| 조기 발견율 (챌린지) | > 70% | discovered_within_3days / total_challenges * 100 |
| 페르소나 다양성 | 균등 분포 | STDEV(persona_distribution) < 3% |
| 중복 발견 비율 | < 10% | duplicate_discoveries / total_discoveries * 100 |
| 댓글 삭제율 | < 2% | deleted_comments / posted_comments * 100 |
| 신규 패턴 발견 | 5개 이상 | COUNT(DISTINCT new_pattern_tags) |

**Python 계산 예시**:
```python
def calculate_weekly_kpis():
    # 1. 트렌드 예측 정확도
    predictions = get_discoveries_by_type('trending_video', days=7)
    validated = [p for p in predictions if still_trending(p, after_days=1)]
    accuracy = len(validated) / len(predictions) * 100

    # 2. 페르소나 다양성
    persona_dist = get_persona_distribution(days=7)
    std_dev = statistics.stdev(persona_dist.values())

    # 3. 중복 발견 비율
    total = get_total_discoveries(days=7)
    duplicates = find_duplicates(total)
    dup_rate = len(duplicates) / len(total) * 100

    return {
        "trend_accuracy": accuracy,
        "persona_diversity": std_dev,
        "duplicate_rate": dup_rate
    }
```

---

## 대시보드 화면 구성

### 1. 실시간 디바이스 그리드 (30×20)

**레이아웃**:
```
┌────────────────────────────────────────┐
│  AIFarm Device Status (600 devices)    │
├────────────────────────────────────────┤
│  [Phoneboard 1]  [Phoneboard 2]  ...   │
│  ████████████████ ████████████████      │
│  20/20 online    18/20 online          │
│                                        │
│  [Phoneboard 3]  [Phoneboard 4]  ...   │
│  ████████████████ ████████████████      │
│  20/20 online    19/20 online          │
│  ...                                   │
└────────────────────────────────────────┘
```

**각 셀 색상**:
- 🟢 녹색: 활동 중 (active)
- 🟡 노란색: 대기 중 (idle)
- 🔴 빨강색: 에러/오프라인 (error/offline)
- 🔵 파랑색: 휴식 중 (resting)

**호버 시 툴팁**:
```
Device ID: 10.0.10.1
Status: Active
Current Activity: persona_commenter
Last Seen: 2025-12-28 12:35:42
Today Completed: 24 tasks
Errors: 1
Battery: 65% (38°C)
```

**구현 (React 예시)**:
```jsx
function DeviceGrid() {
  const [devices, setDevices] = useState([]);

  useEffect(() => {
    // 30초마다 갱신
    const interval = setInterval(() => {
      fetch('/api/dashboard/realtime')
        .then(res => res.json())
        .then(data => setDevices(data.devices));
    }, 30000);

    return () => clearInterval(interval);
  }, []);

  return (
    <div className="device-grid">
      {devices.map(device => (
        <DeviceCell
          key={device.id}
          device={device}
          onClick={() => showDeviceDetails(device)}
        />
      ))}
    </div>
  );
}
```

---

### 2. 활동별 파이차트

**표시 내용**:
- 현재 활성 디바이스의 활동 분포
- 6대 활동별 색상 구분
- 애니메이션: 실시간 업데이트

**차트 구성**:
```
     Shorts 리믹스 (150대, 25%)  🟣
     플레이리스트 (80대, 13%)    🟠
     페르소나 코멘터 (200대, 33%) 🔵
     트렌드 스카우트 (100대, 17%) 🟢
     챌린지 헌터 (50대, 8%)      🟡
     썸네일 랩 (20대, 3%)         🔴
```

**구현 (Chart.js)**:
```javascript
const activityChart = new Chart(ctx, {
  type: 'doughnut',
  data: {
    labels: [
      'Shorts 리믹스',
      '플레이리스트',
      '페르소나 코멘터',
      '트렌드 스카우트',
      '챌린지 헌터',
      '썸네일 랩'
    ],
    datasets: [{
      data: [150, 80, 200, 100, 50, 20],
      backgroundColor: [
        '#9333EA', // 보라
        '#F59E0B', // 오렌지
        '#3B82F6', // 파랑
        '#10B981', // 녹색
        '#EAB308', // 노랑
        '#EF4444'  // 빨강
      ]
    }]
  },
  options: {
    responsive: true,
    plugins: {
      legend: { position: 'right' }
    }
  }
});
```

---

### 3. 시간대별 활동 그래프

**X축**: 시간 (00:00-23:59)
**Y축**: 활동 디바이스 수
**선 그래프**: 6개 활동별 색상 구분
**음영 영역**: 계획 대비 실제 활동 강도

**차트 예시**:
```
600 ┤                    ╭─────╮
    │                    │     │
500 ┤                  ╭─╯     ╰─╮
    │                  │         │
400 ┤            ╭─────╯         ╰────╮
    │            │                     │
300 ┤        ╭───╯                     ╰───╮
    │        │                             │
200 ┤    ╭───╯                             ╰──╮
    │    │                                    │
100 ┤ ╭──╯                                    ╰─╮
    │ │                                          │
  0 ┼─┴──────────────────────────────────────────┴─
    00  03  06  09  12  15  18  21  24

Legend:
━ Shorts 리믹스  ━ 플레이리스트  ━ 페르소나 코멘터
━ 트렌드 스카우트  ━ 챌린지 헌터  ━ 썸네일 랩
```

**구현 (ApexCharts)**:
```javascript
const options = {
  chart: { type: 'line' },
  series: [
    { name: 'Shorts 리믹스', data: hourlyData.shorts },
    { name: '플레이리스트', data: hourlyData.playlist },
    { name: '페르소나 코멘터', data: hourlyData.commenter },
    { name: '트렌드 스카우트', data: hourlyData.trend },
    { name: '챌린지 헌터', data: hourlyData.challenge },
    { name: '썸네일 랩', data: hourlyData.thumbnail }
  ],
  xaxis: {
    categories: Array.from({ length: 24 }, (_, i) => `${i}:00`)
  }
};
```

---

### 4. 발견물 피드 (실시간)

**카드 형태**:
```
┌─────────────────────────────────────┐
│ [썸네일]  viral_short               │
│           "강아지 댄스 챌린지"       │
│           👁 1.5M  ⭐ 8.7            │
│           2분 전 | Device 10.0.10.5 │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ [썸네일]  trending_video            │
│           "신상 게임 꿀팁"           │
│           👁 2.5M  ⭐ 8.9  📈 7위    │
│           5분 전 | Device 10.0.30.12│
└─────────────────────────────────────┘
```

**기능**:
- 최신 20개 표시
- 자동 스크롤 (5초마다 새로고침)
- 클릭 시 YouTube 링크 오픈
- 필터: 타입별, 점수별

**구현 (React)**:
```jsx
function DiscoveryFeed() {
  const [discoveries, setDiscoveries] = useState([]);

  useEffect(() => {
    const ws = new WebSocket('ws://localhost:8080/ws/discoveries');

    ws.onmessage = (event) => {
      const newDiscovery = JSON.parse(event.data);
      setDiscoveries(prev => [newDiscovery, ...prev].slice(0, 20));
    };

    return () => ws.close();
  }, []);

  return (
    <div className="discovery-feed">
      {discoveries.map(d => (
        <DiscoveryCard key={d.id} discovery={d} />
      ))}
    </div>
  );
}
```

---

### 5. 에러 알림 패널

**우선순위별 색상**:
- 🔴 Critical: 디바이스 다운, 네트워크 장애
- 🟡 Warning: 과열, 높은 에러율
- 🔵 Info: 일반 알림

**알림 예시**:
```
┌─────────────────────────────────────┐
│ 🔴 Critical                         │
│ Device 10.0.10.1 offline            │
│ 12:34:56                            │
│ [재시작] [무시] [상세보기]          │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 🟡 Warning                          │
│ Device 10.0.20.5 overheating (47°C) │
│ 12:30:12                            │
│ [휴식] [무시] [상세보기]            │
└─────────────────────────────────────┘
```

**조치 버튼**:
- 재시작: 원격으로 디바이스 재시작
- 휴식: 30분 활동 중지
- 무시: 알림 무시
- 상세보기: 로그 확인

---

### 6. 일일/주간 리포트

#### 일일 요약
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  AIFarm 일일 리포트
  날짜: 2025-12-28
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 활동 통계
  총 활동:     10,523건 (목표 105% ✅)
  발견물:        487건 (목표 97%)
  댓글:        2,834건 (목표 113% ✅)
  평균 점수:      7.8

📈 발견물 타입별
  viral_short:        152건 (31%)
  trending_video:      98건 (20%)
  rising_channel:      45건 (9%)
  challenge_content:   38건 (8%)
  high_engagement:     87건 (18%)
  thumbnail_winner:    67건 (14%)

⭐ TOP 5 발견물
  1. "강아지 댄스 챌린지" (9.2점)
  2. "신상 게임 꿀팁" (8.9점)
  3. "직장인 5분 명상" (8.7점)
  4. "10초 댄스 챌린지" (8.5점)
  5. "엑셀 꿀팁" (8.7점)

🤖 페르소나 성과
  초보자:     15.2% (429건)
  열정팬:     12.1% (342건)
  유머러스:   13.4% (380건)
  조언자:     11.8% (334건)
  관찰자:     12.9% (365건)
  ...

⚠️ 에러
  총 에러:      342건 (3.2% ✅)
  네트워크:      45건
  디바이스:      12건
  기타:         285건

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 주간 트렌드
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  AIFarm 주간 리포트
  기간: 2025-12-22 ~ 2025-12-28
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📅 일별 활동 추이
  월: 10,234건  화: 10,567건  수: 10,123건
  목:  9,876건  금: 11,234건  토: 12,345건  일: 11,987건

📊 카테고리별 발견물
  게임:         28%
  엔터테인먼트: 22%
  교육:         18%
  음악:         12%
  뉴스:          8%
  기타:         12%

🏆 베스트 페르소나 (좋아요 획득 기준)
  1. 유머러스:   평균 3.2개/댓글
  2. 열정팬:     평균 2.8개/댓글
  3. 공감형:     평균 2.5개/댓글

📈 성장 지표
  주간 발견물:   3,412건 (+12% vs 전주)
  주간 댓글:    19,834건 (+8% vs 전주)
  평균 점수:      7.6 (+0.2 vs 전주)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## API 엔드포인트 (대시보드용)

### 1. 실시간 디바이스 상태
```
GET /api/dashboard/realtime
```

**Response**:
```json
{
  "devices": [
    {
      "id": "10.0.10.1",
      "status": "active",
      "current_activity": "persona_commenter",
      "last_seen": "2025-12-28T12:30:45Z",
      "today_completed": 24,
      "errors": 1,
      "battery_level": 65,
      "battery_temp": 38
    },
    ...
  ],
  "summary": {
    "active": 570,
    "idle": 25,
    "error": 3,
    "offline": 2
  }
}
```

**캐싱**: 30초

---

### 2. 최근 발견물 피드
```
GET /api/dashboard/discoveries/recent?limit=20&type=viral_short
```

**Response**:
```json
{
  "discoveries": [
    {
      "id": "uuid",
      "type": "viral_short",
      "title": "강아지 댄스 챌린지",
      "score": 8.7,
      "url": "https://youtube.com/shorts/...",
      "thumbnail": "https://i.ytimg.com/...",
      "discovered_at": "2025-12-28T12:30:00Z",
      "device_id": "10.0.10.1"
    },
    ...
  ]
}
```

**WebSocket 지원**: `ws://localhost:8080/ws/discoveries`

---

### 3. 금일 통계
```
GET /api/dashboard/stats/today
```

**Response**:
```json
{
  "date": "2025-12-28",
  "total_activities": 10523,
  "discoveries": 487,
  "comments": 2834,
  "avg_score": 7.8,
  "device_uptime": 95.2,
  "errors": 342,
  "error_rate": 3.2,
  "breakdown": {
    "viral_short": 152,
    "trending_video": 98,
    "rising_channel": 45,
    "challenge_content": 38,
    "high_engagement": 87,
    "thumbnail_winner": 67
  }
}
```

---

### 4. 주간 통계
```
GET /api/dashboard/stats/weekly?start=2025-12-22&end=2025-12-28
```

**Response**:
```json
{
  "start_date": "2025-12-22",
  "end_date": "2025-12-28",
  "daily_breakdown": [
    { "date": "2025-12-22", "activities": 10234, "discoveries": 476 },
    { "date": "2025-12-23", "activities": 10567, "discoveries": 492 },
    ...
  ],
  "top_discoveries": [
    { "title": "...", "score": 9.2, "url": "..." },
    ...
  ],
  "persona_performance": {
    "초보자": { "count": 2998, "avg_likes": 1.8 },
    ...
  },
  "category_distribution": {
    "게임": 28,
    "엔터테인먼트": 22,
    ...
  }
}
```

---

### 5. 활성 알림
```
GET /api/dashboard/alerts?acknowledged=false
```

**Response**:
```json
{
  "alerts": [
    {
      "id": 1,
      "level": "critical",
      "message": "Device 10.0.10.1 offline",
      "timestamp": "2025-12-28T12:34:56Z",
      "acknowledged": false,
      "actions": ["restart", "ignore", "details"]
    },
    ...
  ]
}
```

**POST /api/dashboard/alerts/{id}/acknowledge**: 알림 확인
**POST /api/dashboard/alerts/{id}/action**: 조치 실행

---

## 대시보드 기술 스택 (권장)

### Frontend
- **React**: UI 컴포넌트
- **TailwindCSS**: 스타일링
- **Chart.js / ApexCharts**: 차트
- **WebSocket**: 실시간 업데이트

### Backend
- **FastAPI** (기존): RESTful API
- **WebSocket**: 실시간 피드
- **Redis**: 캐싱 (30초)
- **PostgreSQL**: 데이터 저장

### 배포
- **Nginx**: 리버스 프록시
- **Docker**: 컨테이너화 (선택)
